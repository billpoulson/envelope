# Public path for the SPA when served by FastAPI at `/app` (see app/main.py, Dockerfile).
# Ensures asset URLs are `/app/assets/…` so reloads on deep routes still load JS/CSS.
VITE_ADMIN_BASENAME=/app
